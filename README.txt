This project was made during freeCodeCamp course of responsive Web Design.

1. Declare the doctype <!DOCTYPE html> of an <html> document.
2. Define the <head> and <body> of an HTML document.
3. Headline with the <h1> element.
4. Headline with the <h2> element.
5. Inform with the <p> paragraph element.
6. Fill in the blank with placeholder "Lorem ipsum" text.
7. Comment out HTML with <!-- -->.
8. Define main content with <main> element.
9. Add images to website using <img> with src and alt attributes.
10. Link to external pages using <a> anchor element with href attribute.
11. Link to internal sections of a page using <a> anchor element with href and id attributes.
12. Nest an <a>anchor element within a <p> paragraph with target and href attributes.
13. Make dead links using the # Hash symbol and href attribute.
14. Turn an image into a link using <a> anchor and href attribute.
15. Create a bulleted unordered list with  <ul> and <li> elements.
16. Create an ordered list with <ol> and <li> elements.
17. Create a text field using <input> element with type attribute.
18. Add placeholder text to a text field using <input> element with placeholder attribute.
19. Create a <form> element using action attribute and <input> element.
20. Add a Submit button to a form using <button> element with type attribute.
21. Use HTML5 to require form field using required attribute.
22. Create a set of Radio buttons using <label> and <input> elements with for, id, type and name attributes.
23. Create a set of Checkboxes using <label> and <input> elements with for, id, type and name attributes.
24. Use the value attribute with Radio buttons and Checkboxes.
25. Check Radio buttons and Checkboxes by Default using checked attribute.
26. Nest many elements within a single <div> element.